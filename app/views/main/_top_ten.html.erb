<%if collection_of_media.length != 0%>
<ul>
  <% collection_of_media.each do |work|%>
  <li>
    <h4>
      <%=link_to work.title, work_path(work)%>
      <small>by <%= work.creator%></small>
    </h4>
    <p>
      <%=work.vote.count%> votes
    </p>
  </li>
  <%end%>
</ul>
<%end%>
