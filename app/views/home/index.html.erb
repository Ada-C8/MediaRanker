<h1>This is the Home Page</h1>

<ul>
  <% @books.each do |book| %>
    <li>
      <%= book.title %>
    </li>
  <%end%>
</ul>

<ul>
  <% @albums.each do |album| %>
    <li>
      <%= album.title %>
    </li>
  <%end%>
</ul>
