<!-- <h3>Top <%# "#{media.upcase}s"%></h3> -->

<ul>
  <% media.each do |work| %>
  <li>
    <h4><%= link_to "#{work.title}", work_path(work) %><small> by <%= work.creator %></small></h4>
    <p>
      <%= work.votes.count %> votes
    </p>
  </li>
  <%end%>
</ul>
