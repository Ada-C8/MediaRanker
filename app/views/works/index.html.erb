<h3>List of works</h3>
<%# TODO: add locals to render different data in each table! %>
<h4>Movies</h4>
<%= render partial: "work_table", locals: {type: "movie", model: @movies} %>
<h4>Books</h4>
<%= render partial: "work_table", locals: {type: "book", model: @books } %>
<h4>Albums</h4>
<%= render partial: "work_table", locals: {type: "album", model: @albums}%>

<%= link_to "View top media", root_path, class: "button" %>

<%= link_to "Add a new work", new_work_path, class: "button"  %>
