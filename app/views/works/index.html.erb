
<section>
    <h3>
      List of Works
    </h3>
      <h4>Albums</h4>
      <%= render partial: 'list', locals: { works: Work.all_albums } %>

      <h4>Books</h4>
      <%= render partial: 'list', locals: { works: Work.all_books  } %>

      <h4>Movies</h4>
      <%= render partial: 'list', locals: { works: Work.all_movies } %>

</section>
