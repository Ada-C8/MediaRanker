<h3>Lists of works</h3>
<h4> Albums </h4>
<table>
  <tr>
    <th>Votes</th>
    <th>Title</th>
    <th>Created By</th>
    <th>Published</th>
    <th>Upvote</th>
  </tr>

  <% @albums.each do |work| %>
  <tr>
    <td> <%= work.votes.count %> </td>
    <td><%= link_to work.title, work_path(work.id) %></td>
    <td><%= work.creator %></td>
    <td><%= work.publication_year %></td>
    <td><a class="button upvote" rel="nofollow" data-method="post" <%= link_to 'Upvote', create_vote_path(work_id: work.id), method: :post%></a></td>
  </tr>
  <% end %>
</table>

<h4> Books</h4>
<table>
  <tr>
    <th>Votes</th>
    <th>Title</th>
    <th>Created By</th>
    <th>Published</th>
    <th>Upvote</th>
  </tr>

  <% @books.each do |work| %>
  <tr>
    <td> <%= work.votes.count %> </td>
    <td><%= link_to work.title, work_path(work.id) %></td>
    <td><%= work.creator %></td>
    <td><%= work.publication_year %></td>
    <td><a class="button upvote" rel="nofollow" data-method="post" <%= link_to 'Upvote', create_vote_path(work_id: work.id), method: :post%></a></td>
  </tr>
  <% end %>
</table>

<h4> Movies </h4>
<table>
  <tr>
    <th>Votes</th>
    <th>Title</th>
    <th>Created By</th>
    <th>Published</th>
    <th>Upvote</th>
  </tr>

  <% @movies.each do |work| %>
  <tr>
    <td> <%= work.votes.count %> </td>
    <td><%= link_to work.title, work_path(work.id) %></td>
    <td><%= work.creator %></td>
    <td><%= work.publication_year %></td>
    <td><a class="button upvote" rel="nofollow" data-method="post" <%= link_to 'Upvote', create_vote_path(work_id: work.id), method: :post%></a></td>
  </tr>
  <% end %>
</table>

<a class="button" <%= link_to "View top Media", root_path%></a>
<a class="button"<%= link_to "Add a new work", new_work_path %></a>
