<h2>List of works</h2>
<% @works.each do |category, works| %>
  <h3><%= category.titleize %>s</h3>
  <table>
    <tr>
      <th>Votes</th>
      <th>Title</th>
      <th>Created by</th>
      <th>Published</th>
      <th>Upvote</th>
    </tr>
    <% works.each do |work| %>
      <tr>
        <td><%= work.votes.length %></td>
        <td><%= link_to work.title, work_path(work.id) %></td>
        <td><%= work.creator %></td>
        <td><%= work.publication_year %></td>
        <td>INSERT BUTTON LATER</td>
      </tr>
    <% end %>
  </table>
<% end %>
