<h3>List of works</h3>

<h4>Albums</h4>
<%= render partial: 'table', locals: { media: @albums } %>

<h4>Books</h4>
<%= render partial: 'table', locals: { media: @books } %>

<h4>Movies</h4>
<%= render partial: 'table', locals: { media: @movies } %>
