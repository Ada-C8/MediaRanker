<main>
  <h3>List of Works </h3>

  <section>
    <h4>Albums</h4>
    <table>
      <thead>
        <tr>
          <th>Votes</th>
          <th>Title</th>
          <th>Created By</th>
          <th>Published</th>
          <th>Upvote</th>
        </tr>
      </thead>
      <tbody>
        <% @works.where(category: "album").each do |work| %>
          <tr>
            <td><%= Vote.where(work_id: work.id).count %></td>
            <td><%= link_to work.title, work_path(work.id)%></td>
            <td><%= work.creator %></td>
            <td><%= work.publication_year %></td>
            <%################## HOW DO I MAKE THIS INTO NOT HASH ROCKET SYNTAX????? %>
            <td>
              <%= link_to "Upvote", votes_path(:vote => {
              user_id: session[:logged_in_session], work_id: work.id
              }), method: :post, class:"button" %>
            </td>
          </tr>
        <% end %>
      </tbody>

    </table>
  </section>

  <section>
    <h4>Books</h4>
    <table>
      <thead>
        <tr>
          <th>Votes</th>
          <th>Title</th>
          <th>Created By</th>
          <th>Published</th>
          <th>Upvote</th>
        </tr>
      </thead>
      <tbody>
        <% @works.where(category: "book").each do |work| %>
          <tr>
            <td><%= Vote.where(work_id: work.id).count %></td>
            <td><%= work.title %></td>
            <td><%= work.creator %></td>
            <td><%= work.publication_year %></td>
            <td>
              <%= link_to "Upvote", votes_path(:vote => {
              user_id: session[:logged_in_session], work_id: work.id
              }), method: :post %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </section>

  <section>
    <h4>Movies</h4>
    <table>
      <thead>
        <tr>
          <th>Votes</th>
          <th>Title</th>
          <th>Created By</th>
          <th>Published</th>
          <th>Upvote</th>
        </tr>
      </thead>
      <tbody>
        <% @works.where(category: "movie").each do |work| %>
          <tr>
            <td><%= Vote.where(work_id: work.id).count %></td>
            <td><%= work.title %></td>
            <td><%= work.creator %></td>
            <td><%= work.publication_year %></td>
            <td>
              <%= link_to "Upvote", votes_path(:vote => {
              user_id: session[:logged_in_session], work_id: work.id
              }), method: :post %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </section>
</main>
