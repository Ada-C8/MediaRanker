<h3>List of Works</h3>

<section>

  <h4>Albums</h4>
  <table>
    <thead>
      <tr>
        <th>Vote </th>
        <th>Title</th>
        <th>Created By</th>
        <th>Published</th>
        <th>Upvote</th>
      </tr>
    </thead>
    <tbody>
      <% @works.each do |work| %>
      <%  if work.category == "album" %>
          <tr>
          <td><%= 0 %> </td>
          <td><%= work.title %></td>
          <td><%= work.creator %></td>
          <td><%= work.publication_year %></td>
          <td><%= "Upvote" %></td>
        </tr>
        <% end %>
        <% end %>
      </tbody>
    </table>
  </section>

  <section>

    <h4>Books</h4>
    <table>
      <thead>
        <tr>
          <th>Vote </th>
          <th>Title</th>
          <th>Created By</th>
          <th>Published</th>
          <th>Upvote</th>
        </tr>
      </thead>
      <tbody>
        <% @works.each do |work| %>
        <%  if work.category == "book" %>
            <tr>
            <td><%= 0 %> </td>
            <td><%= work.title %></td>
            <td><%= work.creator %></td>
            <td><%= work.publication_year %></td>
            <td><%= "Upvote" %></td>
          </tr>
          <% end %>
          <% end %>
      </tbody>
    </table>
  </section>

  <section>

    <h4>Movies</h4>

    <table>
      <thead>
        <tr>
          <th>Vote </th>
          <th>Title</th>
          <th>Created By</th>
          <th>Published</th>
          <th>Upvote</th>
        </tr>
      </thead>
      <tbody>
        <% @works.each do |work| %>
        <%  if work.category == "movie" %>
            <tr>
            <td><%= 0 %> </td>
            <td><%= work.title %></td>
            <td><%= work.creator %></td>
            <td><%= work.publication_year %></td>
            <td><%= "Upvote" %></td>
          </tr>
          <% end %>
          <% end %>
      </tbody>
    </table>
  </section>
