<h3>List of works</h3>
<%# TODO: add locals to render different data in each table! %>
<%= render partial: "work_table", locals: {type: "movie"} %>
<%= render partial: "work_table", locals: {type: "book"} %>
<%= render partial: "work_table", locals: {type: "albums"}%>

<%= link_to "View top media", root_path, :html => { :class => "button" } %>

<%= link_to "Add a new work", new_work_path, :html => {:class => "button"} %>
