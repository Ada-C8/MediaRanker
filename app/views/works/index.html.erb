
<section>
    <h3>
      List of Works
    </h3>
      <h4>Albums</h4>
      <%= render partial: 'list', locals: { works: Work.where(:category => "album") } %>

      <h4>Books</h4>
      <%= render partial: 'list', locals: { works: Work.where(:category => "book")  } %>

      <h4>Movies</h4>
      <%= render partial: 'list', locals: { works: Work.where(:category => "movie")  } %>

</section>
