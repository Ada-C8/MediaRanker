<h2>List of Works</h2>

<table>
  <tr>
    <th>Votes</th>
    <th>Title</th>
    <th>Created By</th>
    <th>Published</th>
    <th>Upvote</th>
  </tr>
  <% @works.each do |work| %>
  <tr>
    <td>num of votes goes here</td>
    <td><%= link_to "#{work.title}", work_path(work.id), method: :get %></td>
    <td><%= work.creator %></td>
    <td><%= work.publication_year %></td>
    <td>upvote button goes here</td>
  </tr>
  <% end %>
</table>

<%= link_to "View Top Media", root_path, method: :get %>
<%= link_to "Add a new work", new_work_path, method: :get %>
