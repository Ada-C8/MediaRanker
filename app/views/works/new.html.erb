<section class="">
  <h2>Add a new work</h2>

  <!-- <% if @work.errors.any? %>
  <p><%= flash[:messages] %></p>
    <ul class="errors">
      <% @work.errors.each do |column, message| %>
      <li> <strong><%= column.capitalize %> </strong> <%= message %></li>
      <% end %>
    </ul>
  <% end %> -->

  <%= render partial: "form", locals: { submit_name: "Create Work" }%>

</section>
