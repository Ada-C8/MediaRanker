<section class="flash">
  <% flash.each do |name, message| %>
    <div class="<%= name %>">
      <h3><%= message %></h3>
      <ul class="errors">
        <% if @work %>
          <% @work.errors.each do |column, message| %>
            <li>
              <%= "#{column}: #{message}" %>
            </li>
          <% end %>
        <% end %>
      </ul>
    </div>
  <% end %>
</section>
