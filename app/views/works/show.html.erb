<main>

<article>
  <h3><%= @work.title %></h3>
  <p><%= @work.creator %></p>
  <p><%= @work.publication_year %></p>
  <p><%= @work.description %></p>

</article>
<article>
  <a><%= link_to("Back to media ranks", root_path, alt: "link to media ranker home page") %></a>
  <a><%= link_to("Edit", root_path, alt: "link to edit work") %></a>
  <a><%= link_to("Upvote", root_path, alt: "link to upvote this work") %></a>
  <a><%= link_to("Delete", root_path, alt: "link to delete this work") %></a>


</article>
  <h3></h3>
  <table>
    <thead>
      <tr>
        <th>User</th>
        <th>Date</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <!-- # TODO Need to make this link to User Summary -->
        <td><a></a></td>
        <td></td>
      </tr>
    </tbody>
  </table>
</main>
