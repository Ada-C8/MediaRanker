  <main class = "row">
  <h1><%= @work.title %></h1>
    <%= link_to "Edit", edit_work_path(@work.id), class: "button"%>
    <%= link_to "Delete", work_path(@work.id), method: :delete,  data: {confirm: "Are you sure?"}, class: "button"%>
    <%= link_to "Upvote", vote_path(@work.id), method: :post , class: "button"%></td>

  <table>
      <tr>
        <th>Title</th>
        <th>By</th>
        <th>Description</th>
        <th>Released</th>
        <th>Votes</th>
      </tr>
      <tr>
        <td> <%=@work.title%> </td>
        <td> <%=@work.creator%> </td>
        <td> <%=@work.description%> </td>
        <td> <%=@work.publication_year%> </td>
        <td> <%=@work.votes_count%> </td>
      </tr>
  </table>

<%= link_to "Back to Media Ranks", root_path %>
</main>
