<div class="row">
<h2 class="small-3 columns"> User Summary: <%= @users[:username] %></h2>
<p class="small-3 columns">Joined site: <%= @users[:created_at].strftime("%B %d, %Y") %></p>

<ul>
  <li><%= link_to "See all Users", users_path %></li>
  <li><%= link_to "Back to Media List", root_path %></li>
</ul>

<h2 class="small-3 row">Votes</h2>

<table class="small-6 columns">
  <thead>
    <tr>
      <th>Media Title</th>
      <th>Created By</th>
      <th>Published</th>
      <th>Category</th>
      <th>Voted on</th>
    </tr>
  </thead>
  <tbody>
      <% @user.votes.each do |vote| %>
    <tr>
      <td><%= link_to vote.work.title, work_path(vote.work_id) %></td>
      <td><%= vote.work.creator %></td>
      <td><%= vote.work.publication_year %></td>
      <td><%= vote.work.category %></td>
      <td><%= vote.created_at.strftime("%B %d, %Y") %></td>
      <% end %>
    </tr>
  </tbody>
</table>
