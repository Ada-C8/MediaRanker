<main>
  <h3>List of Users</h3>
  <table>

    <thead>
      <tr>
        <th>Username</th>
        <th>Votes</th>
        <th>Joined</th>
      </tr>
    </thead>

    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.name %></td>
          <td><%= Vote.where(user_id: user.id).count %></td>
          <td><%= user.created_at %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  
  <%= link_to "Back to Media List", works_path %>
</main>
