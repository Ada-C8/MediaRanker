
<h3>List of Users</h3>

  <% if @users.size == 0 %>
    <p>There are no users yet!</p>
  <% else %>
  <table>
    <thead>
      <tr>
        <th>Username</th>
        <th>Votes</th>
        <th>Joined</th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |u| %>
      <tr>
        <td><%= link_to u.name, user_path(u.id) %></td>
        <td><%= link_to u.name, user_path(u.id) %></td>
        <td><%= link_to u.name, user_path(u.id) %></td>
      </tr>
        <% end %>
      <% end %>
    </tbody>
  </table>
