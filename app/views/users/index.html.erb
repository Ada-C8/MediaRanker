<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>


<h1>Users</h1>
<ul>
  <% @user.each do |user| %>
  <li>
    <%= link_to(user.id, user_path(user.id), alt:'User ID')%>
  </li>
  <li>
    <%= link_to(user.name, user_path(user.id), alt:'User Name')%>
  </li>
  <% end %>
</ul>
