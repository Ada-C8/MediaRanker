require "test_helper"

describe VotesController do
  # it "should successfully create a vote" do
  #   work = works(:harrypot).id
  #   user = users(:user1).id
  #
  #   proc { post votes_path, params: { vote: {user_id: user, work_id: work} }}.must_change 'Vote.count', 1
  #
  # end
end
