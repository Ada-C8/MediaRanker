require "test_helper"

describe VotesController do

  it "should be able to create a new vote" do
    # work_id = Work.last.id
    # post login_path, params: { username: users(:test_user).username }
    # post votes_path(work_id)
    # must_respond_with :redirect
    # must_redirect_to work_path(work_id)

    # proc   {
    #   post votes_path, params: {vote: votes(:test_vote) }
    # }.must_change 'Vote.count', 1
  end

end
