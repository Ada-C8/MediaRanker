require "test_helper"

describe "VotesController" do
# 
#   let(:vote) { Vote.new }
#   let(:fanGirl) { users(:fanGirl) }
#   let(:fanBoy) { users(:fanBoy) }
#   let(:twilight) { works(:twilight) }
#   let(:harry_potter) { works(:harry_potter) }
#
# it "can't vote if not logged in" do
#   vote = Vote.new(user_id: nil, work_id: (:twilight.id))
# vote.must_equal "Must be logged in to vote"
#   end
# end
#
# it "logged in and vote, vote count increases by one" do
# post login_path(user: (:fanGirl.name)
# proc {post create_vote_path(one.id, album.id)}.must_change 'Vote.count', +1
# must_respond_with :redirect
# flash[:success].must_equal "Successfully upvoted!"
#
# end

end
